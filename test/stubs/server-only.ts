// Stub for Next.js's `server-only` package so server modules can be imported
// in the Vitest (Node) environment. The real package throws when bundled for
// the client; under test there is no client bundle to guard against.
export {};
